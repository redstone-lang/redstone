# Types

Redstone has a single primitive type: the 64-bit signed integer.

All literals, variables, function parameters, and return values are `i64`.

```
fn main() {
    let x = 42;      // i64
    let y = -7;      // i64
    return 0;
}
```

There are no strings, booleans, floats, or user-defined types.
